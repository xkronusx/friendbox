version: "3.9"

# All containers join the medianet bridge so they resolve each other by name.
# Traefik is fully optional — when not selected, containers expose direct host ports.
# When Traefik is selected and running, labels route all traffic through Traefik HTTPS.
# traefik.enable=true on all containers — the profiles mechanism gates what actually starts.

networks:
  medianet:
    external: true

services:

  # ── Traefik — reverse proxy + automatic TLS ─────────────────────────────────
  traefik:
    image: traefik:v3.6.9
    container_name: traefik
    profiles: [traefik]
    restart: unless-stopped
    stop_grace_period: 15s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      interval: 30s
      timeout: 5s
      retries: 3
    ports:
      - "80:80"
      - "443:443"
      - "127.0.0.1:8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ${CONFIG_ROOT}/traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - ${CONFIG_ROOT}/traefik/acme.json:/etc/traefik/acme.json
      - ${CONFIG_ROOT}/traefik/dynamic:/etc/traefik/dynamic:ro
    environment:
      # DNS challenge provider credentials — only the active provider's vars are used
      - CF_DNS_API_TOKEN=${CF_DNS_API_TOKEN:-}
      - CF_API_EMAIL=${CF_API_EMAIL:-}
      - DUCKDNS_TOKEN=${DUCKDNS_TOKEN:-}
      - GODADDY_API_KEY=${GODADDY_API_KEY:-}
      - GODADDY_API_SECRET=${GODADDY_API_SECRET:-}
      - NAMECHEAP_API_USER=${NAMECHEAP_API_USER:-}
      - NAMECHEAP_API_KEY=${NAMECHEAP_API_KEY:-}
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      # Dashboard router — https://traefik.${DOMAIN}
      - "traefik.http.routers.dashboard.rule=Host(`traefik.${DOMAIN}`)"
      - "traefik.http.routers.dashboard.entrypoints=websecure"
      - "traefik.http.routers.dashboard.tls=true"
      - "traefik.http.routers.dashboard.tls.certresolver=letsencrypt"
      - "traefik.http.routers.dashboard.service=api@internal"
      - "traefik.http.routers.dashboard.middlewares=auth"
      - "traefik.http.middlewares.auth.basicauth.users=${TRAEFIK_AUTH}"
      # Bare domain redirect — https://${DOMAIN} → https://portainer.${DOMAIN}
      - "traefik.http.routers.root.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.root.entrypoints=websecure"
      - "traefik.http.routers.root.tls=true"
      - "traefik.http.routers.root.tls.certresolver=letsencrypt"
      - "traefik.http.routers.root.middlewares=redirect-root"
      - "traefik.http.routers.root.service=api@internal"
      - "traefik.http.middlewares.redirect-root.redirectregex.regex=^https://${DOMAIN}(.*)"
      - "traefik.http.middlewares.redirect-root.redirectregex.replacement=https://portainer.${DOMAIN}$${1}"
      - "traefik.http.middlewares.redirect-root.redirectregex.permanent=true"

  # ── Portainer — Docker management UI ───────────────────────────────────────
  portainer:
    image: portainer/portainer-ce:latest
    container_name: portainer
    profiles: [portainer]
    restart: unless-stopped
    stop_grace_period: 10s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ${CONFIG_ROOT}/portainer:/data
    ports:
      - "9000:9000"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.portainer.rule=Host(`portainer.${DOMAIN}`)"
      - "traefik.http.routers.portainer.entrypoints=websecure"
      - "traefik.http.routers.portainer.tls=true"
      - "traefik.http.routers.portainer.tls.certresolver=letsencrypt"
      - "traefik.http.services.portainer.loadbalancer.server.port=9000"

  # ── Plex — media playback ───────────────────────────────────────────────────
  plex:
    image: lscr.io/linuxserver/plex:latest
    container_name: plex
    profiles: [plex]
    restart: unless-stopped
    stop_grace_period: 30s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - VERSION=docker
      - PLEX_CLAIM=${PLEX_CLAIM:-}
      - ADVERTISE_IP=https://plex.${DOMAIN}:443
    volumes:
      - ${CONFIG_ROOT}/plex:/config
      - ${CONFIG_ROOT}/plex-transcode:/transcode
      - ${MEDIA_ROOT}/tv:/tv
      - ${MEDIA_ROOT}/movies:/movies
    ports:
      - "32400:32400"
      - "32410:32410/udp"
      - "32412:32412/udp"
      - "32413:32413/udp"
      - "32414:32414/udp"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.plex.rule=Host(`plex.${DOMAIN}`)"
      - "traefik.http.routers.plex.entrypoints=websecure"
      - "traefik.http.routers.plex.tls=true"
      - "traefik.http.routers.plex.tls.certresolver=letsencrypt"
      - "traefik.http.services.plex.loadbalancer.server.port=32400"

  # ── Jellyfin — open-source media server ────────────────────────────────────
  jellyfin:
    image: lscr.io/linuxserver/jellyfin:latest
    container_name: jellyfin
    profiles: [jellyfin]
    restart: unless-stopped
    stop_grace_period: 30s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - JELLYFIN_PublishedServerUrl=https://jellyfin.${DOMAIN}
    volumes:
      - ${CONFIG_ROOT}/jellyfin:/config
      - ${CONFIG_ROOT}/jellyfin-cache:/cache
      - ${MEDIA_ROOT}/tv:/data/tv
      - ${MEDIA_ROOT}/movies:/data/movies
    ports:
      - "8096:8096"
      - "7359:7359/udp"
      - "1900:1900/udp"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.jellyfin.rule=Host(`jellyfin.${DOMAIN}`)"
      - "traefik.http.routers.jellyfin.entrypoints=websecure"
      - "traefik.http.routers.jellyfin.tls=true"
      - "traefik.http.routers.jellyfin.tls.certresolver=letsencrypt"
      - "traefik.http.services.jellyfin.loadbalancer.server.port=8096"

  # ── Sonarr — TV show management ─────────────────────────────────────────────
  sonarr:
    image: lscr.io/linuxserver/sonarr:latest
    container_name: sonarr
    profiles: [sonarr]
    restart: unless-stopped
    stop_grace_period: 30s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_ROOT}/sonarr:/config
      - ${MEDIA_ROOT}/tv:/tv
      - ${MEDIA_ROOT}/downloads:/downloads
    ports:
      - "8989:8989"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.sonarr.rule=Host(`sonarr.${DOMAIN}`)"
      - "traefik.http.routers.sonarr.entrypoints=websecure"
      - "traefik.http.routers.sonarr.tls=true"
      - "traefik.http.routers.sonarr.tls.certresolver=letsencrypt"
      - "traefik.http.services.sonarr.loadbalancer.server.port=8989"

  # ── Radarr — movie management ───────────────────────────────────────────────
  radarr:
    image: lscr.io/linuxserver/radarr:latest
    container_name: radarr
    profiles: [radarr]
    restart: unless-stopped
    stop_grace_period: 30s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_ROOT}/radarr:/config
      - ${MEDIA_ROOT}/movies:/movies
      - ${MEDIA_ROOT}/downloads:/downloads
    ports:
      - "7878:7878"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.radarr.rule=Host(`radarr.${DOMAIN}`)"
      - "traefik.http.routers.radarr.entrypoints=websecure"
      - "traefik.http.routers.radarr.tls=true"
      - "traefik.http.routers.radarr.tls.certresolver=letsencrypt"
      - "traefik.http.services.radarr.loadbalancer.server.port=7878"

  # ── Prowlarr — indexer management (nightly) ─────────────────────────────────
  prowlarr:
    image: lscr.io/linuxserver/prowlarr:nightly
    container_name: prowlarr
    profiles: [prowlarr]
    restart: unless-stopped
    stop_grace_period: 15s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_ROOT}/prowlarr:/config
    ports:
      - "9696:9696"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.prowlarr.rule=Host(`prowlarr.${DOMAIN}`)"
      - "traefik.http.routers.prowlarr.entrypoints=websecure"
      - "traefik.http.routers.prowlarr.tls=true"
      - "traefik.http.routers.prowlarr.tls.certresolver=letsencrypt"
      - "traefik.http.services.prowlarr.loadbalancer.server.port=9696"

  # ── Bazarr — subtitle management ────────────────────────────────────────────
  bazarr:
    image: lscr.io/linuxserver/bazarr:latest
    container_name: bazarr
    profiles: [bazarr]
    restart: unless-stopped
    stop_grace_period: 15s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_ROOT}/bazarr:/config
      - ${MEDIA_ROOT}/movies:/movies
      - ${MEDIA_ROOT}/tv:/tv
    ports:
      - "6767:6767"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.bazarr.rule=Host(`bazarr.${DOMAIN}`)"
      - "traefik.http.routers.bazarr.entrypoints=websecure"
      - "traefik.http.routers.bazarr.tls=true"
      - "traefik.http.routers.bazarr.tls.certresolver=letsencrypt"
      - "traefik.http.services.bazarr.loadbalancer.server.port=6767"

  # ── Ombi — media request manager (v3) ──────────────────────────────────────
  ombi:
    image: lscr.io/linuxserver/ombi:latest
    container_name: ombi
    profiles: [ombi]
    restart: unless-stopped
    stop_grace_period: 10s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_ROOT}/ombi:/config
    ports:
      - "3579:3579"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.ombi.rule=Host(`ombi.${DOMAIN}`)"
      - "traefik.http.routers.ombi.entrypoints=websecure"
      - "traefik.http.routers.ombi.tls=true"
      - "traefik.http.routers.ombi.tls.certresolver=letsencrypt"
      - "traefik.http.services.ombi.loadbalancer.server.port=3579"

  # ── Overseerr — media request & discovery manager ───────────────────────────
  overseerr:
    image: lscr.io/linuxserver/overseerr:latest
    container_name: overseerr
    profiles: [overseerr]
    restart: unless-stopped
    stop_grace_period: 10s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_ROOT}/overseerr:/config
    ports:
      - "5055:5055"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.overseerr.rule=Host(`overseerr.${DOMAIN}`)"
      - "traefik.http.routers.overseerr.entrypoints=websecure"
      - "traefik.http.routers.overseerr.tls=true"
      - "traefik.http.routers.overseerr.tls.certresolver=letsencrypt"
      - "traefik.http.services.overseerr.loadbalancer.server.port=5055"

  # ── NZBGet — Usenet download client ────────────────────────────────────────
  nzbget:
    image: lscr.io/linuxserver/nzbget:latest
    container_name: nzbget
    profiles: [nzbget]
    restart: unless-stopped
    stop_grace_period: 20s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_ROOT}/nzbget:/config
      - ${MEDIA_ROOT}/downloads:/downloads
    ports:
      - "6789:6789"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.nzbget.rule=Host(`nzbget.${DOMAIN}`)"
      - "traefik.http.routers.nzbget.entrypoints=websecure"
      - "traefik.http.routers.nzbget.tls=true"
      - "traefik.http.routers.nzbget.tls.certresolver=letsencrypt"
      - "traefik.http.services.nzbget.loadbalancer.server.port=6789"

  # ── qBittorrent — torrent client (no VPN) ──────────────────────────────────
  qbittorrent:
    image: lscr.io/linuxserver/qbittorrent:latest
    container_name: qbittorrent
    profiles: [qbittorrent]
    restart: unless-stopped
    stop_grace_period: 20s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - WEBUI_PORT=8080
      - TORRENTING_PORT=6881
    volumes:
      - ${CONFIG_ROOT}/qbittorrent:/config
      - ${MEDIA_ROOT}/downloads:/downloads
    ports:
      - "8082:8080"
      - "6881:6881"
      - "6881:6881/udp"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.qbt.rule=Host(`qbt.${DOMAIN}`)"
      - "traefik.http.routers.qbt.entrypoints=websecure"
      - "traefik.http.routers.qbt.tls=true"
      - "traefik.http.routers.qbt.tls.certresolver=letsencrypt"
      - "traefik.http.services.qbt.loadbalancer.server.port=8080"

  # ── qBittorrentVPN — torrent client behind VPN ─────────────────────────────
  qbittorrentvpn:
    image: binhex/arch-qbittorrentvpn:latest
    container_name: qbittorrentvpn
    profiles: [qbittorrentvpn]
    restart: unless-stopped
    stop_grace_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    cap_add:
      - NET_ADMIN
    privileged: true
    sysctls:
      - net.ipv4.conf.all.src_valid_mark=1
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - VPN_ENABLED=yes
      - VPN_PROV=${VPN_PROV:-pia}
      - VPN_CLIENT=${VPN_CLIENT:-openvpn}
      - VPN_USER=${VPN_USER:-changeme}
      - VPN_PASS=${VPN_PASS:-changeme}
      - VPN_OPTIONS=
      - LAN_NETWORK=${LAN_NETWORK:-192.168.1.0/24}
      - NAME_SERVERS=1.1.1.1,1.0.0.1
      - WEBUI_PORT=8080
      - STRICT_PORT_FORWARD=yes
      - ENABLE_PRIVOXY=no
      - UMASK=002
    volumes:
      - ${CONFIG_ROOT}/qbittorrentvpn:/config
      - ${MEDIA_ROOT}/downloads:/downloads
      - /etc/localtime:/etc/localtime:ro
    ports:
      - "8181:8080"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.qbtvpn.rule=Host(`qbtvpn.${DOMAIN}`)"
      - "traefik.http.routers.qbtvpn.entrypoints=websecure"
      - "traefik.http.routers.qbtvpn.tls=true"
      - "traefik.http.routers.qbtvpn.tls.certresolver=letsencrypt"
      - "traefik.http.services.qbtvpn.loadbalancer.server.port=8080"

  # ── DelugeVPN — Deluge torrent client behind VPN ───────────────────────────
  delugevpn:
    image: binhex/arch-delugevpn:latest
    container_name: delugevpn
    profiles: [delugevpn]
    restart: unless-stopped
    stop_grace_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    cap_add:
      - NET_ADMIN
    privileged: true
    sysctls:
      - net.ipv4.conf.all.src_valid_mark=1
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - VPN_ENABLED=yes
      - VPN_PROV=${VPN_PROV:-pia}
      - VPN_CLIENT=${VPN_CLIENT:-openvpn}
      - VPN_USER=${VPN_USER:-changeme}
      - VPN_PASS=${VPN_PASS:-changeme}
      - VPN_OPTIONS=
      - LAN_NETWORK=${LAN_NETWORK:-192.168.1.0/24}
      - NAME_SERVERS=1.1.1.1,1.0.0.1
      - DELUGE_DAEMON_LOG_LEVEL=info
      - DELUGE_WEB_LOG_LEVEL=info
      - ENABLE_PRIVOXY=no
      - UMASK=002
    volumes:
      - ${CONFIG_ROOT}/delugevpn:/config
      - ${MEDIA_ROOT}/downloads:/downloads
      - ${MEDIA_ROOT}/downloads/incomplete:/data/incomplete
      - /etc/localtime:/etc/localtime:ro
    ports:
      - "8112:8112"
      - "58846:58846"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.delugevpn.rule=Host(`deluge.${DOMAIN}`)"
      - "traefik.http.routers.delugevpn.entrypoints=websecure"
      - "traefik.http.routers.delugevpn.tls=true"
      - "traefik.http.routers.delugevpn.tls.certresolver=letsencrypt"
      - "traefik.http.services.delugevpn.loadbalancer.server.port=8112"

  # ── NetbootXYZ — network boot server ───────────────────────────────────────
  netbootxyz:
    image: lscr.io/linuxserver/netbootxyz:latest
    container_name: netbootxyz
    profiles: [netbootxyz]
    restart: unless-stopped
    stop_grace_period: 10s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_ROOT}/netbootxyz:/config
      - ${MEDIA_ROOT}/netboot/assets:/assets
    ports:
      - "69:69/udp"
      - "3000:3000"
      - "8083:8083"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.netbootxyz.rule=Host(`netboot.${DOMAIN}`)"
      - "traefik.http.routers.netbootxyz.entrypoints=websecure"
      - "traefik.http.routers.netbootxyz.tls=true"
      - "traefik.http.routers.netbootxyz.tls.certresolver=letsencrypt"
      - "traefik.http.services.netbootxyz.loadbalancer.server.port=3000"

  # ── TeamSpeak 6 — voice communication server ────────────────────────────────
  teamspeak6:
    image: teamspeaksystems/teamspeak6-server:latest
    container_name: teamspeak6
    profiles: [teamspeak6]
    restart: unless-stopped
    stop_grace_period: 20s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    environment:
      - TZ=${TZ}
      - TS_LICENSE=accept
    volumes:
      - ${CONFIG_ROOT}/teamspeak6:/var/ts6
    ports:
      - "9987:9987/udp"
      - "10011:10011"
      - "30033:30033"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.ts6.rule=Host(`ts6.${DOMAIN}`)"
      - "traefik.http.routers.ts6.entrypoints=websecure"
      - "traefik.http.routers.ts6.tls=true"
      - "traefik.http.routers.ts6.tls.certresolver=letsencrypt"
      - "traefik.http.services.ts6.loadbalancer.server.port=10080"

  # ── Mumble — open-source voice chat server ──────────────────────────────────
  mumble:
    image: lscr.io/linuxserver/mumble-server:latest
    container_name: mumble
    profiles: [mumble]
    restart: unless-stopped
    stop_grace_period: 15s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - SUPERUSER_PASSWORD=${MUMBLE_SUPERUSER_PASSWORD:-changeme}
    volumes:
      - ${CONFIG_ROOT}/mumble:/config
    ports:
      - "64738:64738"
      - "64738:64738/udp"

  # ── Jellyseerr — Jellyfin-native media request manager ─────────────────────
  jellyseerr:
    image: fallenbagel/jellyseerr:latest
    container_name: jellyseerr
    profiles: [jellyseerr]
    restart: unless-stopped
    stop_grace_period: 10s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    environment:
      - TZ=${TZ}
    volumes:
      - ${CONFIG_ROOT}/jellyseerr:/app/config
    ports:
      - "5056:5055"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.jellyseerr.rule=Host(`jellyseerr.${DOMAIN}`)"
      - "traefik.http.routers.jellyseerr.entrypoints=websecure"
      - "traefik.http.routers.jellyseerr.tls=true"
      - "traefik.http.routers.jellyseerr.tls.certresolver=letsencrypt"
      - "traefik.http.services.jellyseerr.loadbalancer.server.port=5055"

  # ── AMP (AMP-MC) — game server management panel ────────────────────────────
  ampmc:
    image: mitchtalmadge/amp-dockerized:latest
    container_name: ampmc
    profiles: [ampmc]
    restart: unless-stopped
    stop_grace_period: 20s
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - medianet
    environment:
      - TZ=${TZ}
      - USERNAME=${AMP_USER:-admin}
      - PASSWORD=${AMP_PASS:-changeme}
    volumes:
      - ${CONFIG_ROOT}/ampmc:/home/amp/.ampdata
    ports:
      - "8085:8080"
      - "25565:25565"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=medianet"
      - "traefik.http.routers.ampmc.rule=Host(`amp.${DOMAIN}`)"
      - "traefik.http.routers.ampmc.entrypoints=websecure"
      - "traefik.http.routers.ampmc.tls=true"
      - "traefik.http.routers.ampmc.tls.certresolver=letsencrypt"
      - "traefik.http.services.ampmc.loadbalancer.server.port=8080"
